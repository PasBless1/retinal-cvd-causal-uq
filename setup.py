from setuptools import setup, find_packages

setup(
    name="retinal-cvd-causal-uq",
    version="0.1.0",
    description="Retinal CVD risk: causal mediation + uncertainty quantification",
    packages=find_packages(),
    python_requires=">=3.9",
)
